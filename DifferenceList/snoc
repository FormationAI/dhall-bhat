  λ(a : Type)
→ λ(xs : ./Type a)
→ λ(y : a)
→ (./../Function/category).op
  (List a)
  (List a)
  (   λ(r : Type)
    → λ(arrowsOut : ∀(z : Type) → (z → List a) → (List a → z) → r)
    → arrowsOut (List a) xs (λ(ys : List a) → [ y ] # ys)
  )
