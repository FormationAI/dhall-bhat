--   λ(object : Kind)
-- → λ(arrow : object → object → Type)
-- → λ(semigroupoid : ./../Semigroupoid/Type object arrow)
-- → λ(m : object → object)
-- → λ(monad : ./../Monad/Type object arrow m)
-- → ./../Category/extractSemigroupoid
--   object
--   (./type object arrow m)
--   (./category object arrow semigroupoid m monad)
<>
