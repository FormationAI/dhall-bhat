./../Comonad/fromExtractExtend
Type
./../Function/Type
./../Function/category
./Type
(λ(a : Type) → λ(nel : ./Type a) → nel.head)
(   λ(b : Type)
  → λ(a : Type)
  → λ(f : ./Type a → b)
  → λ(nel : ./Type a)
  → { head = f nel, tail = [] : List b }
)
