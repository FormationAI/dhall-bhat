    let State = ./Type

in  let execState
        : ∀(s : Type) → ∀(a : Type) → State s a → s → s
        =   λ(s : Type)
          → λ(a : Type)
          → λ(state : State s a)
          → λ(new : s)
          → (state.runState new).state

in  execState
