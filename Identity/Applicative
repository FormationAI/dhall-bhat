  λ(object : Kind)
→ λ(arrow : object → object → Type)
→ λ(category : ./../Category/Type object arrow)
→   { identity = category.identity, op = category.identity }
  : ./../Applicative/Type object arrow (./Type object)
