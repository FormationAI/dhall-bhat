  λ(object : Kind)
→ λ(cat : ./../Category/Monoidal/Type object)
→ λ(category : ./../Category/Type object cat.arrow)
→ λ ( bifunctor
    : ./../Functor/Bifunctor/Type
      object
      object
      object
      cat.arrow
      cat.arrow
      cat.arrow
      cat.op
    )
→ λ(m : object)
→ λ(group : ./Type object cat m)
→ category.op
  (cat.op m m)
  m
  (   λ(r : Type)
    → λ ( arrowsOut
        : ∀(z : object) → cat.arrow z m → cat.arrow (cat.op m m) z → r
        )
    → arrowsOut
      (cat.op m m)
      group.op
      ( ( ./../Functor/Bifunctor/impliedFirstFunctor
          object
          cat.arrow
          object
          cat.arrow
          category
          object
          cat.arrow
          cat.op
          bifunctor
          m
        ).map
        m
        m
        group.inverse
      )
  )
