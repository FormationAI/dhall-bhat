  λ(object : Kind)
→ λ(arrow : object → object → Type)
→ λ(t : (object → object) → object → object)
→ { lift :
        ∀(m : object → object)
      → ∀(monad : ./../Monad/Type object arrow m)
      → ∀(a : object)
      → arrow (m a) (t m a)
  }
