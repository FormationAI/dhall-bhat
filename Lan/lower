  λ(f : Type → Type)
→ λ(applicative : ./../Applicative/Type f)
→ λ(g : Type → Type)
→ λ(functor : ./../Functor/Type g)
→ λ(a : Type)
→ λ(lan : ./Type f g a)
→ lan
  (g a)
  (   λ(b : Type)
    → λ(k : f b → a)
    → λ(x : g b)
    → functor.map b a (λ(y : b) → k (applicative.pure b y)) x
  )
