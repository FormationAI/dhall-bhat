    let Either = ./../Either/Type

in    λ(f : Type → Type)
    → { compact :
          ∀(a : Type) → f (Optional a) → f a
      , separate :
          ∀(a : Type) → ∀(b : Type) → f (Either a b) → { _1 : f a, _2 : f b }
      }
