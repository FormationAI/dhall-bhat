    let Applicative = ./../Applicative/Type

in    λ(f : Type → Type)
    → λ(c : ./Type f)
    → λ(applicative : Applicative f)
    → λ(a : Type)
    → λ(b : Type)
    → λ(k : f (a → Optional b))
    → λ(fa : f a)
    → c.compact b (applicative.ap a (Optional b) k fa)
