    let Monad = ./../Monad/Type

in    λ(f : Type → Type)
    → λ(c : ./Type f)
    → λ(monad : Monad f)
    → λ(a : Type)
    → λ(b : Type)
    → λ(k : a → f (Optional b))
    → λ(fa : f a)
    → c.compact b (monad.bind a (Optional b) fa k)
