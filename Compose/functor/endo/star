  λ(m : Type → Type)
→     let MFunctor = ./../../../Functor/Endo/Star m
  
  in    λ(f : Type → Type)
      → λ(fStar : MFunctor f)
      → λ(g : Type → Type)
      → λ(gStar : MFunctor g)
      →   { map =
                λ(a : Type)
              → λ(b : Type)
              → λ(fn : a → m b)
              → fStar.map (g a) (g b) (gStar.map a b fn)
          }
        : MFunctor (./../../type f g)
