--   λ(object : Kind)
-- → λ(arrow : object → object → Type)
-- → λ(f : object → object)
-- → λ(applicative : ./Type object arrow f)
-- → applicative.op applicative.identity : f (a -> b) -> f a -> f b
<>
