    let object = Type

in  let cat = ./../Category/Monoidal/Set/cartesian

in    λ(m : object)
    → λ(field : ./Type m)
    →   { additive =
            field.additive
        , multiplicative =
            ./../Group/Commutative/extractMonoid m field.multiplicative
        }
      : ./../Ring/Type m
