    let object = Type

in  let cat = ./../../Category/Monoidal/Set/cartesian

in    λ(m : object)
    → λ(monoid : ./Type m)
    → monoid.{ op } : ./../../Semigroup/Type object cat.arrow cat.product m
